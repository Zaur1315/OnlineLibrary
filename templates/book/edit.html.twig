{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}

    <div class="container mt-5 mb-5">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body mt-5 mb-5">
                        <h1>Редактирование книги "{{ book.title }}"</h1>

                        {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

                        <a href="{{ path('app_book_index') }}"class="btn btn-secondary mt-3">Назад</a>

                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
